# =============================================================================
# Tmux Configuration - Minimal Setup
# =============================================================================

# Set prefix to Ctrl-b (default)
set-option -g prefix C-b

# Enable mouse support
set -g mouse on

# Set terminal colors to 256-color mode
set -g default-terminal "screen-256color"

# Increase history limit
set -g history-limit 5000

# =============================================================================
# Key Bindings
# =============================================================================

# Split panes with | and -
bind | split-window -h
bind - split-window -v

# Navigate panes with arrow keys
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Create new window with c
bind c new-window

# Close window/pane with x
bind x kill-pane

# Reload config with r
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# =============================================================================
# Status Bar
# =============================================================================
# Status bar will be managed by tmux-powerline plugin
# Config is at ~/.config/tmux-powerline/config.sh
# Theme is at ~/.config/tmux-powerline/themes/steam-dark.sh
# Segments are at ~/.config/tmux-powerline/segments/

# =============================================================================
# Pane Border Colors
# =============================================================================

# Steam dark theme borders
set -g pane-border-style fg="#0E1419"
set -g pane-active-border-style fg="#00BFFF"

# =============================================================================
# Tmux Plugin Manager (TPM)
# =============================================================================

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'erikw/tmux-powerline'

# Initialize TPM (must be at the bottom of config)
# This loads all plugins and their configurations
run '~/.tmux/plugins/tpm/tpm'
